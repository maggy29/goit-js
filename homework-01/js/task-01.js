'use strict';

const name = 'Генератор защитного поля';
let price = 1000;

const message = `Выбран <<${name}>>, цена ${price} кредитов`;

console.log(message);

price = 2000;

const messageNewPrice = `Выбран <<${name}>>, цена ${price} кредитов`;

console.log(messageNewPrice);